//
// Info.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Info: Codable, JSONEncodable, Hashable {

    public enum PackageName: String, Codable, CaseIterable {
        case basic = "Basic"
        case pro = "Pro"
        case ultra = "Ultra"
        case mega = "Mega"
        case enterprise = "Enterprise"
    }
    /** The package that you are using. */
    public var packageName: PackageName
    /** The usable coins list for your package. */
    public var usableCoins: [String]
    /** Maximum allowed candles for your package to fetch at one query. */
    public var maxCandlesOnRequest: Int
    /** The timeframes that you can select on your queries. */
    public var usableTimeframes: [String]
    /** Whether you can select start and end dates during queries. */
    public var customDateSelection: Bool

    public init(packageName: PackageName, usableCoins: [String], maxCandlesOnRequest: Int, usableTimeframes: [String], customDateSelection: Bool) {
        self.packageName = packageName
        self.usableCoins = usableCoins
        self.maxCandlesOnRequest = maxCandlesOnRequest
        self.usableTimeframes = usableTimeframes
        self.customDateSelection = customDateSelection
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case packageName
        case usableCoins
        case maxCandlesOnRequest
        case usableTimeframes
        case customDateSelection
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(packageName, forKey: .packageName)
        try container.encode(usableCoins, forKey: .usableCoins)
        try container.encode(maxCandlesOnRequest, forKey: .maxCandlesOnRequest)
        try container.encode(usableTimeframes, forKey: .usableTimeframes)
        try container.encode(customDateSelection, forKey: .customDateSelection)
    }
}

