//
// Metadata.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct Metadata: Codable, JSONEncodable, Hashable {

    /** The trading pair symbol. */
    public var symbol: String
    /** The timeframe used for the candles. */
    public var timeframe: String
    /** ISO timestamp of the first available candle. */
    public var firstCandle: Date
    /** ISO timestamp of the last available candle. */
    public var lastCandle: Date
    /** Total number of available candles. */
    public var totalCandles: Int
    /** Completion percentage (0–100) as a string with two decimals. */
    public var completionPercent: String
    /** Estimated date when full data collection will complete. */
    public var estimatedCompletionDate: Date

    public init(symbol: String, timeframe: String, firstCandle: Date, lastCandle: Date, totalCandles: Int, completionPercent: String, estimatedCompletionDate: Date) {
        self.symbol = symbol
        self.timeframe = timeframe
        self.firstCandle = firstCandle
        self.lastCandle = lastCandle
        self.totalCandles = totalCandles
        self.completionPercent = completionPercent
        self.estimatedCompletionDate = estimatedCompletionDate
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case symbol
        case timeframe
        case firstCandle
        case lastCandle
        case totalCandles
        case completionPercent
        case estimatedCompletionDate
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(symbol, forKey: .symbol)
        try container.encode(timeframe, forKey: .timeframe)
        try container.encode(firstCandle, forKey: .firstCandle)
        try container.encode(lastCandle, forKey: .lastCandle)
        try container.encode(totalCandles, forKey: .totalCandles)
        try container.encode(completionPercent, forKey: .completionPercent)
        try container.encode(estimatedCompletionDate, forKey: .estimatedCompletionDate)
    }
}

